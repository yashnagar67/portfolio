# Yash Nagar Portfolio

To run this project locally:

1. Extract the ZIP
2. Open the folder in VS Code
3. Run `npm install`
4. Run `npm run dev`
5. Open the local URL provided by Vite.

Note: You will need a Google Gemini API Key in your environment variables (`VITE_API_KEY`) to use the chat feature locally.